# Used for initial bootstrap.
# Ignored by ACM policy.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: open-cluster-management

resources:
  - namespace.yaml
  - operatorgroup.yaml
  - subscription.yaml
