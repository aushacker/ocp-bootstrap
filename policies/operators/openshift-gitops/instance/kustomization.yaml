# Used for initial bootstrap.
# Ignored by ACM policy.
---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # - git-repos-template.yaml # Manually update and create repository secrets
  - appproj-cluster-gitops.yaml
  - appproj-policies.yaml
  - argocd.yaml
