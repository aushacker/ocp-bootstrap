kind: ClusterRole
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: argocd-extended-apis
rules:
  - apiGroups:
      - cluster.open-cluster-management.io
    resources:
      - managedclustersets
    verbs:
      - create
      - update
      - delete
